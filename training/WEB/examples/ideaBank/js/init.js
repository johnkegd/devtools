// initialize components
    document.addEventListener('DOMContentLoaded', function() {

        //init carousel full width
        $('.carousel.carousel-slider').carousel({
            fullWidth:true,
            indicators:true
        });

        //init nav section
        $('.sidenav').sidenav();
       

});
